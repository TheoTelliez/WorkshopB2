{% extends "base.html.twig" %}
{% block style %}<link href="css/style-signin.css" rel="stylesheet"> {% endblock %}
{% block title %} {{ parent() }} Ajout Languages {% endblock %}  

{% block contenu %} 

    <div class="wrapper" style="text-align: center">
        <form class="form-signin" action="index.php?page=languages" method="post"
              enctype="application/x-www-form-urlencoded">
            <div class="row">
                <div class="col-lg-12 col-sm-12">
                    <div class="panel panel-default">
                        <!-- Table -->
                        <div class="table-responsive">
                            <table class="table table-hover table-striped">
                                <tr>
                                    <th style="text-align: center">Équipements</th>
                                    <th style="text-align: center">kWh</th>
                                    <th style="text-align: center">Modifier ma consommation journalière / par an</th>


                                </tr>
                                        {% for ld in listeld %}
                                    <tr>

                                        <td>{{ld.nom}}</td>
                                        <td>{{ld.watt}}</td>
                                        <td>

                                            <a href="index.php?page=modifconso&id={{ld.id}}">Modifier

                                            </a>



                                        </td>
                                        <!-- <td><input type="number" name="hpj[]" id="hpj[]" value="0" min="0" max="24" step="0.1"/></td>
                                         <td><input type="number" name="jpa[]" id="jpa[]" value="0" min="0" max="365" step="1"/></td> -->
                                        <input type="hidden" name="idLanguage" id="idLanguage" value="{{ld.nom}}"/>
                                    </tr>

                                        {% endfor %}
                            </table>

                        </div>

                    </div>
                    <!-- {% if listeld != empty %}
                        <a href="index.php?page=languages"><button class="btn btn-lg btn-primary btn-block" type="submit" name="btSave" id="btSave">Sauvegarder</button></a>
                        <br/><br/>
                    {% endif %} -->
                </div>
            </div>

        </form>

    </div>

{% endblock %}